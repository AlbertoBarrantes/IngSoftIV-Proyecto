@model IEnumerable<Negocio.Entities.OrdenCompra>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.estadoOrdenID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.proveedor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fechaOrden)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.estadoOrdenID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.proveedor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fechaOrden)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idOrdenCompra">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idOrdenCompra">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idOrdenCompra">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
